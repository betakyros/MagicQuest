using UnityEngine;

public class MagicProperties : MonoBehaviour {

    public enum MagicColorName { RED, BLUE, GREEN };
    public enum MagicType { ATTACK, TARGET};
    public enum MoveSpeed {DEATH_MOVE_SPEED = 30, ATTACK_MOVE_SPEED = 400};
}
